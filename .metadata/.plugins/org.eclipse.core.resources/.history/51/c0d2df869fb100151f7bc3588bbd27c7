import java.util.Queue;

public class Statement extends Sentence {
	
	public void ReadIn(Queue<String> word) {
		
		/*
		
		if (word is det, adv, adj, or noun)
		{
			NounPhrase nPh;
			nPh.ReadIn(word);
		}
		else if (word is present participle)
		{
			GerundPhrase gerund;
			gerund.ReadIn(word);
		}
		else if (word is "to")
		{
			InfinitivePhrase inf;
			inf.ReadIn(word);
		}
		
		//once that's done, start on the verbs
		if (word is adv or verb)
		{
			Predicate pred;
			pred.ReadIn(word);
		}
		if not, ERROR because you need a damn verb
		
		
		*/
	}

}
